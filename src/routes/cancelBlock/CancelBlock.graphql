query getBlockByReservation($listId: Int) {
    getBlockByReservation(listId: $listId) {
        id
        blockUniqueId
        reservationItemCount
    	reservationItem {	
      		id
    	}
   		sessionTime{
    		date
  		}
    }
}