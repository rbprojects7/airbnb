query MoreListReviews($listId: Int!, $offset: Int){
    moreListReviews(listId: $listId, offset: $offset){
        id
        reservationId
        listId
        authorId
        userId
        reviewContent
        rating
        parentId
        automated
        createdAt
        status
        isAdmin
        authorData {
            profileId
            firstName
            lastName
            picture
        }
    }
}